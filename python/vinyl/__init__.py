from vinyl.keyboard import (key_press, key_down, key_up)
from vinyl.mouse import (left_click, right_click, middle_click,
                         left_double_click,
                         left_up, right_up, middle_up,
                         left_down, right_down, middle_down,
                         move, move_to, mouse_wheel,
                         get_cursor_pos, get_cursor_shape)

from vinyl.time import(sleep)

from vinyl.screen import(screenshot)
